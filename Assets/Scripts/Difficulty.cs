using System;
/// <summary>
/// contains all possible difficulties. Difficulties must be defined in the sort order. The numeric value, corresponding to the difficulty of the recipe, is implied by the order in this enum.
/// </summary>
public enum Difficulty
{
    EASY=1,
    EASY_MEDIUM,
    MEDIUM,
    MEDIUM_HARD,
    HARD
}
